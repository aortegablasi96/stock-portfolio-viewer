# Design Decision Records (DDRs)

This directory holds **design decisions** for Stock Portfolio Viewer — UI/UX and lower-level design
choices that are more granular than full ADRs but still worth recording. In the
documentation hierarchy, DDRs sit just below ADRs in `docs/decisions/`.

DDRs are authored via the `design-recorder` governance skill.

## Conventions

- One file per decision: `NNNN-short-title.md` (e.g. `0001-dashboard-layout.md`).
- Numbers are zero-padded and increase monotonically.
- If a design decision turns out to have architectural impact, promote it to an ADR in
  `docs/decisions/` and reference it here.

## Suggested Template

```markdown
# NNNN. <Title>

- **Status:** Proposed | Accepted | Superseded by NNNN
- **Date:** YYYY-MM-DD

## Context

The design problem and constraints.

## Decision

The chosen design and why.

## Alternatives Considered

Other options and why they were not chosen.
```

## Recorded DDRs

- `0001` — Read-only dashboard layout and data-loading state pattern
- `0002` — Model connection/load outcome as a discriminated-union IPC result
- `0003` — Snapshot persistence model, money representation, and capture policy
- `0004` — Flex import persistence: real-valued multi-currency amounts, append-only, two-tier de-dupe
- `0005` — M3 analytics read model, base-currency conversion, and per-period aggregation
- `0006` — App shell: client-side tab navigation for analytics views
- `0007` — Portfolio display currency: live IBKR FX source + selector/display conventions
- `0008` — Performance view: daily portfolio-value reconstruction from Flex MTM history
- `0009` — Sector classification: gateway-sourced, locally cached; allocation as donut charts
- `0010` — Upcoming dividends from Flex open accruals; net-of-withholding chart reading
- `0011` — Custom frameless window shell with in-app title bar and IPC-driven controls
- `0012` — In-place confirmation pattern for destructive actions (ConfirmAction)
- `0013` — Performance view: cumulative TWR curve, chart tabs, and shared interactive hover
- `0014` — Allocation view: geographic exposure as an equirectangular bubble map *(superseded by 0019 on the basemap; data semantics still apply)*
- `0015` — Allocation view: cash as an asset class, derived from the NAV residual
- `0016` — Dividend rows: shares held reconstructed from trades; per-share divided out of the amount
- `0017` — Analytics tables: time-range filter anchored to the data, composed with the type filter
- `0018` — One shared content measure for the shell; charts sized by aspect ratio
- `0019` — Allocation map: Mapbox basemap with an SVG bubble overlay (supersedes `0014`) *(superseded by 0020 on the overlay; basemap decisions still apply)*
- `0020` — Allocation map: one canvas circle per holding, with a hover popup (supersedes `0019`) *(superseded by 0030 on the unit, spread, canvas layer and per-holding granularity; approximation and a11y stance still apply)*
- `0021` — Allocation map: the gain/loss colour scale (red↔gray↔blue, fixed ±25%; extends `0020`) *(superseded by `0045`; its `--pos`/`--neg` rule still applies)*
- `0022` — Bounded gateway requests; a stalled gateway as its own `not_responding` state (extends `0002`)
- `0023` — Classification refresh: resumable (persist before reporting) and progress-reporting (extends `0009`)
- `0024` — Gateway reads coalesced in the repository behind a short freshness window; FX fetched concurrently (extends `0022`)
- `0025` — Single-instance lock: the second launch quits before touching the database and focuses the first (extends `0011`)
- `0026` — The Flex store describes itself: stored statements + coverage span, read on launch (extends `0004`)
- `0027` — Analytics tabs stay mounted; a re-read becomes explicit and non-destructive (extends `0006`)
- `0028` — Window size/position/maximized remembered in `app_meta`; reachability judged on the title bar (extends `0011`)
- `0029` — Tab shell: the complete ARIA tabs pattern, roving tabindex, automatic activation (extends `0006`)
- `0030` — Allocation map: two donuts per country (country weight + sectors); per-holding marks retired; palette blue reserved from sectors (supersedes `0020`)
- `0031` — Spacing, radius, type and focus scales on `:root`; one focus ring as a zero-specificity base rule (extends `0018`)
- `0032` — One `Button` primitive: seven role-named variants, four sizes, `className` for placement only (extends `0031`)
- `0033` — One `Card` primitive: `default`/`nested` surfaces, `--surface-pad-*` sizes, sentence-case titles, `CardContent` as the body scope (extends `0032`)
- `0034` — One `StatTile`: no surface of its own (the card supplies it), `tone` as the only axis, neutral as the absence of a tone (extends `0033`)
- `0035` — One `Field` owning an explicit `htmlFor` and a generated id; one `.control` box for `Select`/`DateInput`, `kind` as the only axis (extends `0034`) *(amended by `0075`: the sidebar's currency field boxes, and its control gives up its resting border and inline padding — that placement only)*
- `0036` — One `ToggleGroup`: `mode` as the only axis (single-select box vs multi-select pill), `aria-pressed` buttons replacing three tablists that never implemented the pattern (extends `0035`)
- `0037` — One `Badge`: `variant` as boundary and ink, `size` as inline chip vs standalone label; never a pill, because `0036` spent that corner on "pressable" (extends `0036`)
- `0038` — One `StatePanel`: `variant` as the state (loading/empty/notice/error), `surface` as "does it bring a card"; element and ARIA role derived, only `error` paints (extends `0037`)
- `0039` — One `DataTable`: `surface`/`height` on the container (retiring the `.card-content` override), sorting opt-in per column, missing values last in both directions (extends `0038`)
- `0040` — Allocation breakdown: table and donut link on hover; emphasis keyed on slice identity, muting rather than recolouring (extends `0039`)
- `0041` — Map popup return tint: banked into the edges as a gradient whose band is the content padding, so colour never passes behind text (extends `0030`, `0021`)
- `0042` — Token adoption held by a ratchet: `BASELINE` may only shrink and a dead entry fails; exemptions enumerated by hand, because two mechanical rules leaked. Amended by #152, which emptied the baseline and exempted three sub-4px hairlines (extends `0031`)
- `0043` — One `AnalyticsShell` for the four analytics views: a render prop over the report, `subject` as the axis, branch mapping and state wording in a pure module (extends `0038`, `0027`)
- `0044` — Two durations, two easings; reduced motion honoured by redefining the tokens to `0ms` rather than listing what moves, so a raw duration is the only way out (extends `0031`)
- `0045` — Allocation map: one view, coloured by sector; the gain/loss mode and the `--diverge-*` scale withdrawn, the popup's return tint kept (supersedes `0021`, whose `--pos`/`--neg` rule still governs)
- `0046` — Contrast: the loss tone splits (`--neg` for fills, `--neg-text` for text) and `--accent-strong` fills the primary button, so neither validated palette moves; guarded by a Node test that measures hover states too (extends `0021`, `0034`, `0032`)
- `0047` — The Allocation map is a labelled `group`, not an `img`: it must contain Mapbox's attribution links, so the graphics are made inert instead (canvas and markers `tabindex="-1"`, `keyboard: false`); guarded by a comment-stripped text scan (extends `0030`, `0046`)
- `0048` — Tab icons are a second channel beside the label (never its name), one shared `Glyph` frame, sized `1em` from the type scale, drawn in `currentColor`; guarded by a comment-stripped text scan (extends `0029`, `0031`)
- `0049` — Performance daily-return bars: chain-linked from the TWR curve (never differenced from value), thinned rather than aggregated because aggregation destroys the volatility the chart exists to show; own component sharing `columnDomain` and the 4.5:1 ratio (extends `0013`, `0018`)
- `0050` — Daily NAV comes from `EquitySummaryInBase` (already exported, never parsed), so the value curve is the broker's series rather than `0008`'s reconstruction, which stays as the fallback; composition has negative bands below the baseline and an `other` residual that is surfaced, not redistributed — its 100%-stacked axis is superseded by `0052`, the rest stands (supersedes `0008`; extends `0005`, `0015`, `0018`)
- `0051` — Performance: the four charts in a 2×2 grid replacing the switcher; the shared plot re-cut to 500×180 (≈2.78:1) so a half-column keeps its axis labels legible, collapsing to one column at 1200px; geometry shared in `lib/chartGeometry` and checked by a walk over the layout (extends `0018`, `0049`, `0050`) Amended by #190, which found the y-axis gutter had never fitted the label it was sized for — 64 units buys eight characters and every value chart labels its axis with ten, so the `€` was clipped off every tick; now 80, derived from the measured advance and a stated character budget
- `0052` — Composition stacks cumulatively in base currency (top edge = NAV), bands softened by `fill-opacity` on `.stack-band` alone rather than retuning the palette; its legend moves into the card header so all four grid cards are exactly one header + one bare `<svg>` tall; daily-return bars scrubbed by band with an outlined active bar; the two chart source notes deleted (supersedes the 100%-stacked half of `0050`; extends `0051`, `0049`, `0030`)
- `0053` — Inter and JetBrains Mono bundled as repo assets (no font host, no CSP change, no dependency); a figure role that is **one rule** carrying family + `tabular-nums` + `-0.02em` together, its membership taken from the selectors that already declared tabular digits, minus four that render prose (extends `0031`, `0042`, `0018`)
- `0054` — Palette re-keyed to navy/indigo by measurement, not paste: four of the proposal's eleven values failed a guard and were re-derived (`--muted` 3.89:1, `--accent` 4.14:1 as text, and one rose that is right for neither role); the fill/text loss split survives and *inverts*; the eight categorical hues are re-validated and deliberately unchanged, while the three warm achromatic tokens go cool; five rules that hard-coded a colour now mix it from a token (extends `0046`, `0030`, `0031`)
- `0055` — The tablist rotates into a vertical 220px sidebar and stays a tablist: `aria-orientation="vertical"` and Up/Down, Left/Right deliberately inert, views still mounted rather than conditionally rendered; the active row's 2px underline becomes a 3px leading-edge bar plus a weight step, the hover rule is scoped `:not(.app-tab-active)` because `:hover` out-specifies it, and `lib/contrast.ts` learns to measure a `color-mix()` tint. The Performance grid's breakpoint moves 1200px → 1420px to keep meaning the same content column, which costs DDR-0051's "a fresh install opens on the grid" (extends `0029`, `0027`, `0048`, `0051`, `0054`)
- `0056` — The sidebar's context rail: the gateway badge is **derived** from the Portfolio view's own `portfolio:getOverview` result rather than polled (no new channel, no timer against the gateway), and a live reading ages out at five minutes — `Live · 14:32` becomes `Last seen 14:32` — mirroring the repository's session TTL; five outcomes get five wordings against three tones, so no pair is separated by colour alone, and the dot's fill/text split is declared once as two local properties. The display currency moves to the footer and becomes the *app's* selection: never disabled, sequenced instead, and it stops resetting to EUR on every visit. The prototype's account number is deliberately not shown — the overview carries none, and a new figure opens its own story (extends `0055`, `0022`, `0024`, `0027`, `0046`)
- `0057` — Sidebar collapse: one `app-collapsed` flag on the shell rather than a `collapsed` prop anywhere, so the tabs pattern is the same code in both states and selecting a view *cannot* reopen the column; the title bar keeps spanning the window above the sidebar, because with no OS frame it is the only grab handle and `0028` judges reachability on it; collapsed labels are **clipped, not removed**, so a rail row is still named by its own text and `title` stays an addition; the status dot grows a shape channel (filled/hollow/haloed) where its wording is clipped; state persisted as one `app_meta` value beside the window's own, and the grid's two breakpoints derived from one 1200px content column — which gives the 1280px default window its 2×2 grid back (extends `0055`, `0056`, `0011`, `0028`, `0029`, `0044`, `0051`) *(superseded by `0068` on the toggle's placement; everything else still applies)*
- `0058` — One page header for all five views, owned by `AnalyticsShell` (which takes the `<main>` with it) rather than by `App.tsx`'s `AnalyticsPage` — so the title renders in every branch by construction, error and needs-import included; `RefreshBar` dissolves into the header's status row, Portfolio gains a reading time it never had, the status row is absent (not empty) where there is nothing to refresh, and the prototype's repeated app-name eyebrow is dropped because the title bar and the brand tile already carry it (extends `0043`, `0027`, `0038`, `0056`)
- `0059` — The card's ruled header strip: one rule over two selectors (a `CardHeader` and a bare `CardTitle` read identically), bled to the card's edges by negating a `--card-pad` each size restates, with `.card-header:last-child` giving it all back; `CardContent` untouched, so no `StatePanel` moves. The table's column head drops to 11px with the tracking that makes 11px uppercase survive, and every table gains a CSS row lift on `--duration-fast` — the linked row's stronger lift re-scoped to out-weigh it, since the two are the same row whenever the pointer is what linked it. The proposal's `20px 24px` padding and its `--surface-2` are both declined (extends `0033`, `0039`, `0040`, `0044`, `0054`)
- `0060` — The KPI tile's figure goes to the redesign's 26px and the row's column minimum moves with it — one decision in two declarations, with the arithmetic (0.6em digits × a twelve-character figure + two card paddings) asserted rather than commented, and 14.5rem the middle of a 0.4rem window bounded below by the card overrunning and above by Trades' four tiles leaving one row at 1280px; it costs a column at the minimum window, which `auto-fit` is for. `.stat-label` adopts `0059`'s 11px micro-label rather than the proposal's second tracking, so the app has one; the tone axis, the absent surface and the figure role are all untouched (extends `0034`, `0031`, `0059`, `0053`)
- `0061` — The chart language: the area wash becomes a `<linearGradient>` whose stops stay CSS (the `fill` is per-element, because a gradient is referenced by id and two curves share a page), and `LineChart` gains a zero line only where zero is a level the series *crosses* rather than the floor it sits on. Three private `HoverReadout`s become one `ChartTooltip` drawn **inside the `viewBox`** — which is what makes "it cannot cover the chart next door" a construction rather than a tuned offset — pinned to the top of the plot in all three, with a muted sans label beside a right-aligned figure. Bars, `.stack-band`, the donut's native tooltip and the map are all deliberately untouched (extends `0018`, `0051`, `0052`, `0053`, `0044`)
- `0062` — The Portfolio view's rail: the imported store renders *beside* the live states rather than inside the `ok` branch, so a disconnected gateway can't hide the import path; `FlexImport` becomes a hook plus three fragments with the `dataVersion` bump in the hook, where splitting the panel across three grid cells cannot strand it; `SnapshotHistory` survives the prototype's omission and pairs with Stored statements as the app's two local stores; and one `lib/weightBars` scale — the set's maximum with a 25% floor under it — replaces the prototype's two disagreeing hard-coded divisors, drawing the rail's `meter` and the table's `aria-hidden` micro-bar from the same number (extends `0027`, `0012`, `0046`, `0057`)
- `0063` — Allocation's breakdown becomes a *pair*: the donut's column fixed at 280px (a third structural width token) with the table taking every pixel the window gains, `align-items: start`, and a stacking breakpoint derived from what the table needs (948px, asserted rather than commented); the donut's legend returns **wrapping**, carrying a muted name and its share while value stays in the table and the slice's `<title>` — the `showLegend` prop and the vertical legend it guarded were both dead. The breakdown moves above the map, because the map is the tallest card and the only approximate one. The basemap goes `dark-v11`, the decision `0054` deferred here, and it is **two** changes: the weight donut's track was `var(--card)` at 42%, which is a grey ring only when a white map is behind it, so it moves to `--muted` — asserted together, since either alone is a regression that looks deliberate (supersedes `0019`'s style value, not its reasoning; extends `0030`, `0040`, `0054`) *(ordering superseded by `0074`; everything else still applies)*
- `0064` — The prototype's tinted type chips ship as two `Badge` variants that are still a boundary and an ink, not a fill: the tone mixed halfway into `--border`, `--pos` and `--neg-text` as the two inks, and no weight step because a type label cannot stop naming its type. The variant union becomes a **superset** of `STAT_TONES`, so `toneOf()` names a variant directly — and the polarity comes from the row's **signed amount**, never from a list of Flex type strings that is right today and silent when a fourth type arrives. The badge is `sm` (a `md` box grows every row) with the inline gap taken off by a doubled placement class, because the two forms are indistinguishable to CSS: an inline chip follows a *text node*, and `:first-child` counts elements. The income key moves into the card header and `ColumnChart` loses its `<figcaption>` (extends `0037`, `0046`, `0034`, `0052`, `0059`)
- `0065` — Trade side becomes a badge with **no tone at all**: the gain/loss pair is declined because those two hues already mean gain and loss in the same row's last cell, and no substitute hue is invented either — `--accent` means news (`0037`) and the `--series-*` slots are a dimension's categorical set (`0030`), so the badge earns its place on **shape**, a run of boxes where a bare word left the column ragged. No new variant, no new contrast pairing, and no branch on the side string. `.flex-import-dim` moves to `.data-table .data-table-dim` rather than gaining a second copy under a second view's prefix, and the `realizedNative !== 0` predicate written out three times becomes one, so the column cannot tone a row it renders as a dash. The `Best`/`Worst` label is toned by **its own item's figure**, not by which card it labels — composed from `.stat-label` plus the tone rules rather than declared as a third — and the nested card's separation is re-measured and left alone: it was always the border (1.228 → 1.252:1), never the fill (extends `0037`, `0064`, `0034`, `0033`, `0039`, `0021`)
- `0066` — One instrument name across Dividends, Trades and Allocation, decided by whether the description **says anything the symbol does not** rather than by its shape or its asset category: the story proposed a `XXX.YYY` regex for currency pairs, and querying the owner's real statements found a *second* non-company shape — bare codes (`CAD`, `USD`) in `flex_fifo_summaries` that carry realized P&L and so already sit in the realized-gains table — which that regex would have rendered as "Cad". `assetCategory` is declined for `0064`'s reason and because `RealizedBySymbol` does not carry one. `instrumentName` returns **`null`**, not the raw string, so a line repeating the ticker above it is not drawn at all; one four-line `InstrumentName` holds the app's only remaining `.flex-import-file` inside `components/analytics/`, asserted against the **directory** so a fourth view cannot reintroduce the split. The suffix list's `SPA` and `GROUP` judgements are now visible on every instrument and deliberately unchanged (extends `0064`, `0017`, `0029`)
- `0067` — The suffix list was one list doing two jobs, so it becomes two: a **legal form** is never part of a name and goes wherever it trails, repeatedly; a **common noun** (`GROUP`, `HOLDINGS`, `COMPANY`, `CO`) often *is* the name and goes **only where it is the last word** — the owner's call, and the only rule available, since `GOLDMAN SACHS GROUP INC` and `JUROKU FINANCIAL GROUP INC` reach opposite verdicts and a name with a word too many is still that company while a name with a word too few is a different one. Order is load-bearing: the noun is stripped **once, before** any legal form, or removing `INC` promotes `GROUP` into last place and takes it anyway. A stranded leading `THE` goes with the noun it paired with (and only where something *was* stripped, so `THE TORONTO-DOMINION BANK` keeps it); an acronym is a token with **no vowel**, because the case that would tell `LVMH` from `GOLD` is the case being replaced — misses (`AB`, `SAP`) asserted rather than hidden. `SPA` stays a legal form on evidence: two real Italian holdings against a hypothetical spa. Two of seventeen real instrument names change, both `GROUP` retentions, checked by running both versions over the real database (extends `0066`, `0064`)
- `0068` — The collapse toggle moves out of the footer into the sidebar's **head row**, beside the app's name: it is the one control that changes the navigation's width and it sat at the opposite end of the column from it. `0057` measured that placement rather than preferring it — 118px beside a 32px tile and a 30px control, where "Stock Portfolio Viewer" wants 154 — so the measurement is answered rather than ignored: the name **wraps to two lines** on `--leading-tight` and the head grows ~9px, because an abbreviated product name is a worse defect than a taller head row. Widening the column is refused, since `--sidebar-width` is an input to *both* Performance breakpoints and a wider one costs the 1280px window the 2×2 grid collapsing exists to give it back; on the rail the row turns the corner and stacks the tile above the control. The Tab order becomes toggle → tabs → currency → panel — one stop *fewer* between a tab and its panel, not one more (supersedes `0057`'s placement only; extends `0056`, `0055`, `0029`, `0051`)
- `0069` — The gateway badge becomes a **boxed chip** — a fill, a 1px `--border` edge, `--radius-md` and scale padding — with a rule splitting it from the brand row above; the head's padding moves onto its two children, because a 1px delimiter drawn inside a padded box stops 12px short of the column's edges. It is deliberately **not** `Badge`: that primitive is never a background, never a box, and its variants are *data* tones (`0037`). The fill is one new `:root` token, `--surface-raised` (the proposal's own `#161b2e`), and it is **the only surface step in the app that goes up** — reusing `--bg` was the free option and is wrong on placement, since the content column beside the rail *is* `--bg` and a recessed chip reads as a hole rather than a box. `designTokens.test.ts` pins it by *ordering* (lighter than `--card`, dimmer than the `--border` that edges it, within 1.2:1 of `--card`), because the value alone says nothing. Every tone moved onto the new fill, so the three gateway-dot pairings in `lib/contrast.ts` were **re-pointed rather than added to** — a pairing that stopped occurring measures nothing — and three text pairings joined them; each loses headroom, and the warn dot at 3.63:1 is now the tightest measurement in the list. A third threshold, `SURFACE_EDGE` (1.2), covers a **decorative boundary no WCAG bar governs**, calibrated between the app's fill-only step (1.059:1) and the card edge it already ships (1.251:1); its one entry is `--border` on `--card`, which is every card's inner edge and every table rule. On the rail the chip squares to the brand tile's own 32px, padding removed so a clipped 1px `<span>` cannot push the dot off centre; the wording stays clipped, and the prototype's click-to-expand chip stays declined (extends `0056`, `0057`, `0068`, `0046`, `0064`, `0054`, `0037`, `0033`, `0031`)
- `0070` — The chart hover card is restyled without being re-placed: it floats on `--surface-raised`, which becomes the app's **second** use of the one surface step that goes up, with `--border` and `--radius-md`'s step; the padding and the corner stay in **viewBox units** (a `viewBox` has no page-relative length), and a `MIN_WIDTH` floor stops a one-row card reading as a different component from a five-row one — a `max`, asserted at both ends. The substantive half is colour: a composition row's figure takes its **band's own class**, which makes the row, the ribbon and the legend one string rather than three call sites in step — but the `--series-*` slots have been fills since `0030` and three of the eight fail AA as ink (4.39, 4.33, 3.45; `--series-6` fails on `--card` too, at 3.74). So `0046`'s split is applied to a *categorical* set: a derived `--series-ink-*` ramp lifting the floor to 4.87:1, with the eight validated slots and every band untouched, and the mix ratio pinned against the stylesheet. Daily return keeps **one** row against the proposal's two series, toned by sign with a zero day untoned. `contrast.ts`'s "`--neg-text` is never a `fill`" guard is corrected rather than deleted — an SVG `<text>` has no `color` — and now enumerates its one exception (extends `0061`, `0069`, `0046`, `0030`, `0018`, `0031`, `0052`)
- `0071` — Owner feedback against the proposal, in two halves. The **value curve leaves the categorical palette for `--accent`**: slot 1 is the first of eight *categories* and a lone curve is not one of eight of anything, while the proposal's own `#6366f1` stays declined — `0054` measured and rejected it, and readmitting it as a marks-only token would leave a value in `:root` that must never carry a label with only a comment to police it. `--series-1` keeps the Stocks band, the weight donut and slot 1. The **return curve is split at zero** in the daily bars' own `--pos`/`--neg` (the *mark* half; SVG writes both roles as `fill`, so the wrong one is silent), which supersedes `0070`'s "the cumulative curve is deliberately not toned" on the owner's call and against the proposal, which draws it indigo. The split is **geometry, not a second series** — one polyline and one polygon under complementary clips — because splitting the data would mean inventing a crossing sample the report never took; the wash re-anchors from the domain floor to **zero**, or a wholly-underwater stretch fills downward from the curve and a loss is drawn like a gain. `lib/signedCurve` exists because an unclamped clip takes a **negative height** where a range never crosses zero, and SVG then renders *nothing* — the chart loses fill and line in the two ranges nobody scrubs to. The curve's **hover row is toned with it**, through the daily chart's own `signInk` rather than a second copy — a card row had been toned only where its *mark* was, which left this one figure the grid's only untoned signed number. Two things found on screen, not by tests, and now guarded: that gap, and a bare `.chart-mark-pos` tying `.chart-dot` on specificity and losing on source order — `0059`'s trap exactly (supersedes two sentences of `0070`; extends `0061`, `0054`, `0046`, `0030`, `0049`)
- `0072` — A chart title names the chart, not the window: *Performance change over time* stops renaming itself to *…over the selected period*, in a grid whose whole point is cross-reading four cards under one range selection. The resolution is that the disclosure the conditional carried was **never conditional** — the curve opens at 0% under every range, full history being the *identity* case of rebasing rather than a second case — so the invariant half (the baseline) becomes a fixed `BaselineNote` in the card header and the variable half (the window) stays where a variable thing belongs, on the `RangeFilter` and the tiles' hints. "Period" because the tile beside it already says *At period end*; the note wears `.chart-legend-header` rather than a rule differing from it only in name, so that slot now has a swatchless occupant. The `aria-label` stays conditional on purpose — a name is read in place of the plot, and is not a title — leaving the baseline stated twice and neither statement the card's name. The header row is re-measured at 1421px, the narrowest two-column window: no title clips and all four cards are 304px, so `0051`'s equality is untouched (extends `0051`, `0052`, `0059`, `0061`, `0018`)
- `0073` — The composition stack inverts to run **least-invested at the bottom**: Accruals, Cash, the `other` residual, Options, Stocks — one rule placing all five, so the residual sits above the Cash it is not and never takes the chart's floor, and the top edge a reader follows is drawn by the band that accounts for it rather than by a 0.13% sliver. The order is a `Record` keyed by the domain union, so a *new* band fails to compile rather than falling off the picture. The report's `bands` array was doing two jobs — drawing order **and** palette order — so they split: `bands` keeps the palette and `lib/composition`'s `stackOrder` owns the picture, which is why inverting the stack **repainted nothing** and why presentation stops being declared in a service Epic #179 is holding still (reordering `BAND_SPECS` would have given Accruals slot 1's blue). Legend and hover card both read top-down, the sequence they are looked at in. The top band takes the proposal's 0.8→0.5 ramp over its own bounding box (stops in `app.css`, `fill` per element, id from `useId()`), whichever band reaches the top; `#6366f1` stays declined (`0071`) and the proposal's 0.7 flat opacity stays declined too, because `0052` measured 0.5 against the same neighbours. Every band gains a 1px `non-scaling-stroke` of its **own** hue — the opposite decision from the `--card` separator `0052` removed, which painted over the slivers it was meant to delimit. A palette slot now publishes `--series-hue` beside its `fill` and `background`, which is what keeps a stroke and a gradient stop off a second copy of the eight hues, and what forces the ribbon into a `<g>` wrapper (CSS beats a presentation attribute, so a classed ribbon cannot also be filled `url(#…)`). The key's swatch becomes the proposal's 8px square keeping the 12px one's 25% corner and stops being softened — the ratchet's **ninth** exemption, the first that could not be widened into an existing one (extends `0052`, `0050`, `0051`, `0061`, `0070`, `0071`, `0030`, `0042`)
- `0074` — Allocation's map moves **above** the breakdown and pays for the move in height: `aspect-ratio` flattens 3:1 → 4:1 and the floor under it becomes `--country-map-min-height`, the fourth structural measure, so both live on **one** budget — derived in the test from `WINDOW_DEFAULT_HEIGHT` itself and three named page measurements, because a wrapped legend is type on a laid-out page and no Node test can compute it. `0063`'s ordering is superseded and its *reason* is answered rather than dropped. The coupling neither half declares is that **the map's height is what the popup lives in**: Mapbox clips a popup to the map container and flips it above or below the mark only, so a 277px frame truncates a 270px card — measured over all seventeen hover targets against the owner's real import, 69–149px gone, against 56px at 3:1, which no test could ever see. The clip therefore moves **down one level** to the canvas container, taking the rounded corner and keeping the marks in — the one deliberate exception to `0061`'s tooltip-inside-the-plot rule, which a `viewBox` can honour and a vendor-positioned popup cannot. Each mark and each sector slice grows a **bounded** `holdings` list (four names by market value, the rest a count, the weight donut's remainder naming nothing), drawn as `Badge` chips composed through `badgeClassName` in the app's one imperative-DOM popup rather than a fourth chip family; the name is `instrumentName`'s, so a description repeating its symbol is `null` and the chip is the ticker alone. No mark, arc, hue or figure is added — `0030`'s retirement of per-holding granularity is untouched (supersedes `0063`'s ordering only; extends `0030`, `0041`, `0045`, `0047`, `0061`, `0066`, `0037`, `0031`)
- `0075` — The sidebar's nav block gains a **title that is the tablist's own name**: `aria-label="Views"` becomes `aria-labelledby` pointing at visible text, so one region keeps exactly one name rather than gaining a caption beside one — outside the tablist, because every child of `role="tablist"` is a tab, and inside the one 56px clip rule, because removing it would leave the region unnamed on the rail. It is the app's one micro-label, added to `statTileVariants.test.ts`'s existing loop rather than agreeing by inspection, and the proposal's `0.1em` stays declined (`0060`). The rhythm takes the proposal's 11px/9px to the nearest steps (`--space-4`, `--space-3`) and its **2px row gap is never adopted because the scale lands on the same number** — 9+2+9 and 8+4+8 are both 20px, which is the argument for a scale made in one line. The 13px label is declined: `--text-xs` is the *hints* step and these five rows are the primary nav. 220px is **re-derived rather than re-tuned** — measured in the running app against the bundled Inter, a row leaves 152.8px and "Performance" advances 89.35px, so the label was never the binding constraint and the head row still is (`0068`). The display currency becomes `--surface-raised`'s **third** adopter, matching the chip at the column's other end (`0069`), which costs two `contrast.ts` pairings re-pointed off `--card` and moves `sidebarRail.test.ts`'s count to three; the control gives up its resting `border-color` (`transparent`, never `none`, so hover still out-specifies it) and its `padding-inline`, amending `0035` in that one placement. The proposal's 0.65 icon opacity is declined — `currentColor` already makes the step, and dimming a `--muted` graphic buys it under 3:1 — and the footer's rule stays, because the column's head ships a rule *and* a chip. A malformed comment is now a test failure: prose left outside a comment killed `.app-tab`'s whole rule while lint, typecheck, 1178 unit tests and 63 e2e specs all passed — the exact inverse of the comment-stripping trap `0042`/`0047`/`0048`/`0058`/`0070` record, and invisible to every text guard in the repo (amends `0035`; extends `0055`, `0056`, `0057`, `0068`, `0069`, `0070`, `0031`, `0042`, `0060`, `0048`)
- `0076` — The composition bands read as tints of the card rather than slabs, and the change that matters is that the softening is now **measurable**: `lib/contrast.ts` has enumerated every ink in the app since Story #163 and never held one entry for this chart, because a `fill-opacity` is not a colour — it resolves against whatever lies beneath it, which a Node test has no layout engine to determine, so `0052` and `0073` both tuned this card by eye. The proposal's fill numbers are **alpha** and are adopted exactly (0.7 flat, 0.8→0.5 on the top band, reversing `0073`'s declining of the 0.7), while the softening moves into the **hue**: `--band-tint` mixes a band's slot 62.5% into `--card`, derived rather than picked — 0.8 × 62.5% is the 50% every flat band already wore, so the loudest tint the chart renders is the one its quietest band had, and the top band's NAV edge falls 3.70:1 → 2.17:1. The two channels then split by measurement: **the edge carries the band and the fill only decorates it**, so the full-strength stroke is held to `NON_TEXT` on all nine slots (`--series-6` tightest at 3.74:1) and the fill is held *under* that bar, with `contrast.test.ts` asserting the **gap** — every fill quieter than every edge. Diluting the strokes instead is declined on the number: 85% puts `--series-6` at 3.01:1 and 84% fails. `Pairing` gains an optional `maximum`, the module's first non-floor, because every failure it was built for was something too faint and this one is the other kind. The tint is declared on `.stack-hue` beside the palette class on the wrapping `<g>` — not on `.pie-series-*` (a tint of `--card` is no donut's business) and not on `:root` (a custom property computes where it is declared, so `--series-hue` would resolve to nothing) — and the **ribbon drops its palette class**, which it only ever had to be filled by, removing a `fill` that would have tied the tint on specificity and lost on source order (`0059`'s trap). The key's swatch deliberately does **not** follow: it states the hue a band wears on its edge, and that edge is what stayed full strength (supersedes `0073`'s flat-opacity choice and `0052`'s softening mechanism; extends `0070`, `0069`, `0064`, `0061`, `0046`, `0059`, `0054`, `0030`, `0031`, `0051`)
- `0077` — The Dividends key stops naming the two *segments* and names the **column**: the height is the month's gross, which is the figure the card is about and the one the key left unnamed — visible only inside a native `<title>`, under a `totalLabel="Gross"` string literal sitting beside the two constants that existed so the key and the readout could not drift. Three constants now, `IncomeLegend`'s prop renamed `lowerLabel` → `columnLabel` because the meaning is what changed, and no third swatch for *Net* (a key names what a reader cannot otherwise resolve; the net is what is left when the top segment comes off). `ColumnChart` was the app's **last** native `<title>` — `0061`'s card reached the other three in #188 and #220 and this chart took none of it — so it takes the shared card, hit-testing the **band** like the daily-return bars, which is what makes a zero-net month hoverable at all. `stackedTooltipRows` owns the two invisible rules: **an absent row rather than a zero** (a month with nothing withheld has no withholding row, and the card shrinks) while the **total row stays at zero**, because a month whose only entry is withholding has a gross of exactly zero and that is what explains the negative net; and **only the signed row is toned**, `signInk`'s `--neg-text` never `--neg`, with gross and withholding untoned because a deduction's loss tone restates its own label (`0065` from the other direction). The widening-`viewBox` trap is answered by **not correcting for it**, on a fact rather than a preference: `.chart-tooltip-value` is 12 units and `.chart-axis-label` is 11 in *both* plots, so the card already stands in the same relation to the type around it in a 3664-unit plot as in a 1080-unit one — a card scaled to hold a constant size on screen is the version that misbehaves, measured at 2.16× on the owner's real import, which puts its figures at 29.8px against the chart's own 12.6px labels and past ~21 months makes it taller than the plot. `ChartTooltip` therefore gains a `plot` (defaulted, so the three original callers are unchanged) and the chart's geometry moves to `lib/column`'s `columnPlot`, deliberately **not** `chartGeometry` — a grid shares a `viewBox` because it is cross-read; this chart is alone and widens. Recorded rather than acted on: `COLUMN_PLOT_FLOOR` is 1080 because Story #76 matched a line chart that `0051` has since taken to 500×180, so this chart's labels render at half the grid's and a multi-year history flattens the plot to a strip — a re-decision of its aspect, and `0018`'s territory (supersedes `0010`'s series naming; extends `0061`, `0070`, `0064`, `0046`, `0065`, `0049`, `0051`, `0018`)
- `0078` — The income chart stops stacking: a month is **two bars on one baseline**, gross and withholding, because `0077`'s renaming could not fix a geometry where one named series is drawn *as part of* the other — a segment whose size is only readable against a baseline that is not there. The `{ lower, upper }` model becomes `{ primary, secondary }` and the view hands over the report's own `grossBase`/`withholdingBase` instead of mapping `lower: m.netBase`, the indirection that let the key and the picture disagree; `pairedDomain` floors at zero and takes its top from the **taller** of the two, because a month whose only entry is withholding has a gross of zero. The stated cost is Story #49's downward bar: nothing hangs below the line now, that month reads as a tax bar beside **no** income bar, and the signed net lives in the hover card — one baseline is what makes two bars comparable, and a plot cannot have one baseline and a signed series at once. The width policy `0077` deferred is closed by **sizing the plot in pixels** (`plot.width × COLUMN_UNIT_PX`) inside a `.chart-scroll` wrapper, so the plot grows *longer* rather than denser: `COLUMN_UNIT_PX` is **derived** from the two constants that already stated the constraint — an axis label is `AXIS_LABEL_UNITS` and may not render below `MIN_AXIS_LABEL_PX`, both 11, so a unit is a pixel — and is the *smallest* value clearing the floor, every pixel above it being plot the reader must scroll. `min-width: 100%` makes it a floor rather than a fixed size, so a short history still fills its card (`0018`'s objection to a chart floating in a panel) and stretching only ever enlarges. **DDR-0018 is re-decided for this chart alone**: that rule holds where a chart's aspect is a property of the chart, and here it is a property of the *data*. The 1080-unit floor stays and finally earns its reason — not "matches the line chart" (which `0051` moved) but the **card's height**, since a stretching `<svg>` keeps its aspect: a twelve-month floor was implemented, measured at 376px against 282px for the same thirteen bars, and reverted, so the floor is `COLUMN_PLOT_MIN_ASPECT = 4.5` and today's import renders pixel-identical to before. The scroll region is `tabIndex={0}` + `role="group"` + a name (WCAG 2.1.1 — it holds the only copy of the older months), rings from the `:where(...)` base rule, and does overflow and nothing else (`0039`'s split). Recorded and left for its own story: **the value axis scrolls away with the plot**, and pinning the gutter needs a second `<svg>` agreeing on a height that varies with the stretch (supersedes `0077`'s scaling conclusion and its deferred finding; extends `0018`, `0051`, `0049`, `0061`, `0064`, `0065`, `0047`, `0039`)
- `0079` — The income chart's **value axis leaves the `viewBox`** and becomes HTML, closing the gap `0078` recorded: its ticks were `<text>` inside the scrolling `<svg>`, so they slid away and left gridlines that still *look* like an axis. `COLUMN_PLOT_PAD.left` drops 64 → **0** — the space was this axis's — and the gridlines' start, `bandIndexAt`'s origin and the hover card's clamp move in one step. `0018` is re-decided **on its own reasoning**: a label in the `viewBox` scales with the plot, which is right for one attached to the plot's *geometry* and wrong for one attached to its *frame*, so the **month** labels stay in and the value axis comes out — and comes out onto the page type scale (`--text-2xs`, joining the figure role's existing selector list, since a second rule applying it throws), so it stops shrinking at *any* history length and the story **removes** a reason for a ratchet exemption rather than adding one. The offset is a **percentage** of the gutter's height, the only expression that survives a plot whose rendered height the card decides — which makes the gutter's height the whole question, and the obvious answer wrong: placed *beside* the scroller as a grid column the labels measured exact at rest and **~15px low the moment the plot scrolled**, because a scroller's height includes its horizontal scrollbar — exact in the case that doesn't matter, wrong in the case the story exists for. So the gutter is a **sticky flex item *inside* the scroller**: inside, because a percentage resolves against the content box a scrollbar is not in, making it exactly as tall as the `<svg>` by construction; sticky, so the plot slides under it, which is why it takes `--card` as ground. The plot's fill becomes `flex: 1 0 auto` — grow fills what the gutter leaves, and `flex-shrink: 0` is what keeps it scrolling rather than being squeezed back. The column sizes itself from a hidden in-flow copy of its widest tick, making `0051` §#190's hand-cut gutter (eight characters against ten-character labels, clipping the € in silence) unexpressible rather than documented. `COLUMN_PLOT_MIN_ASPECT` is **re-derived** 4.5 → 4.2, not re-tuned: it describes the plot, and the plot lost its gutter, so the same total chart at its floor is 1008 units plus 55px. Measured at 1280/1440/1760, at rest and scrolled to the end: every label lands on its gridline within **0.01px** (supersedes `0078`'s deferred consequence; extends `0018`, `0051`, `0053`, `0061`, `0031`, `0042`, `0039`)
- `0080` — Withholding **hangs below the zero line**, reversing `0078`'s below-zero decision (its width and scroll halves still govern) and restoring Story #49's month. Legal because the negation is on the *series*, never the value — the signed series `0078` refused was a third *net* bar, whose sign moves with the data. The income card's `source-note` goes with it, the scroll disclosure moving into the region's accessible name. Open finding: a small withholding against a large gross over-allocates the negative region.
- `0081` — The income chart's **tax side keeps its own scale**, closing `0080`'s open finding: `columnDomain` rounds both extremes to one shared nice step, which floored a €14.25 withholding to a −€100 axis and gave a series needing 7% of the domain 33% of it. `pairedDomain` now takes the step from the dominant side and rounds the small side to a nice number of its own magnitude, contributing one labelled bottom tick — so the axis is **uneven across zero and nowhere else**, held legible by that label and by zero always being a tick. `columnDomain` is untouched; the daily-return chart's two sides are genuinely comparable.
- `0082` — The hover card is **bounded down the plot, not only across it**: `tooltipLayout` compared its height against nothing, so 8 rows sat over the date labels and 10 were clipped by the `viewBox` — `0051` §#190's failure class in the other axis, with a one-row margin (6 of 7). `maxRowsFor` derives the bound from the card's own metrics; past it the card keeps what fits and spends its last slot on `+N more`, because a truncation is information and silence is not a channel. The component renders `box.rows`, never its prop. The capacity guard binds the `CompositionBand` **enum**, so a new band fails a test rather than the screen.
- `0083` — Switching view stops requiring a walk back to the sidebar: **`Ctrl`/`Cmd` and a view's own digit, from anywhere**, handled by a `window` listener in `App.tsx` rather than by main's `before-input-event` (which would carry a keystroke the renderer already has back across a new channel). The tabs pattern is **extended and not touched** — `nextTabIndex` still declines every key it does not own, and the two handlers never negotiate because they read *different properties*: the tablist reads `key`, the accelerator reads `code`. `code` is not incidental — on AZERTY the unshifted top row is `&`, `é`, `"`, `'`, `(`, so a binding read off `key` would not exist on that keyboard. The jump selects **exactly** its destination, where arrowing selects and mounts every row it crosses (`0027`). Focus moves to the destination tab *by requirement*, not preference: the panel it was standing in is about to be `hidden`, which blurs its contents to `<body>`. The Epic's text-entry rule is adopted as **the rule and not the collision** — a modifier collides with none of `0035`'s three controls, and it is implemented anyway because the rule outlives the binding. The disclosure is **an attribute, not a mark**: each row's `title` becomes *Portfolio (Ctrl+1)*, which **amends `0057`** — a row had a `title` only while its label was clipped, on the reasoning that a tooltip repeating legible text says nothing, and this one does not repeat the label. It stays an addition rather than the mechanism, because a `title` is consulted for a name only where an element has no content, and the clipped label is content — which makes the by-name lookups *sharper* than before, the title no longer being the name. One modifier is written (a tooltip is read, not parsed) from a platform *parameter*, so `shortcutLabel` stays pure and `App.tsx` holds the app's only `navigator` read. A **drawn digit per row was built, reviewed on screen and withdrawn** on the owner's call — permanent weight in the primary navigation for something learned once — which takes its cost back with it: a row is one text node again, so `textContent` is its accessible name, and the four e2e assertions that had briefly moved off it keep `toHaveAccessibleName` as the property they always meant (amends `0057`; extends `0029`, `0055`, `0068`, `0027`, `0035`, `0011`)
- `0084` — The **chart card is the ruled strip's one stated exception**: four Performance charts and the Dividends income card draw no line between a title and a plot, where a full-width rule reads as a second axis. Scoped once on `.chart-card-header`, the class both call sites already wore, so a sixth chart card inherits it by *being* one — a `chart` variant on `Card` would put an existing scope into a component API whose two axes are surface and padding (`0033`), and a `className` at the call sites is decoration (`0032`). **Deleting `border-bottom` alone would not have finished it**: the strip is a geometry as much as a line, so a header keeping the negative `--card-pad` bleed and its re-applied inset holds 36px of nothing under a division no longer drawn. All three declarations go back, in the order `.card-header:last-child` already gives them back, leaving a plain row inside the card's own padding and **one chosen gap** — `--space-5`, the proposal's own 16px and the step a bare `.card-title` already carries. The selector names **both** classes, which is the load-bearing half: `.card-header` and `.chart-card-header` are each one class, so a bare rule would tie the strip and win on source order alone — `0059`'s trap, shipped once as `.chart-mark-pos` against `.chart-dot` — and the guard asserts the compound rule exists *and* that no bare one does. Every other card keeps its strip by never naming the class. Pinned in Playwright because a text guard passes off the source whichever selector was written (`0075`): the chart header off a **probe** wearing its classes inside a real `.card` (the charts need imported history the e2e app has none of, the way `reduced-motion.spec.ts` borrows `.pie-slice`), the ruled half off Portfolio's real "Data sources" — and the border widths are **parsed, not compared**, since a 1px hairline computes as `0.8px` at a 1.25 scale factor (amends `0033`, `0059`; extends `0052`, `0051`, `0032`, `0031`, `0042`, `0075`)
