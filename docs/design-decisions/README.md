# Design Decision Records (DDRs)

This directory holds **design decisions** for Stock Portfolio Viewer — UI/UX and lower-level design
choices that are more granular than full ADRs but still worth recording. In the
documentation hierarchy, DDRs sit just below ADRs in `docs/decisions/`.

DDRs are authored via the `design-recorder` governance skill.

## Conventions

- One file per decision: `NNNN-short-title.md` (e.g. `0001-dashboard-layout.md`).
- Numbers are zero-padded and increase monotonically.
- If a design decision turns out to have architectural impact, promote it to an ADR in
  `docs/decisions/` and reference it here.

## Suggested Template

```markdown
# NNNN. <Title>

- **Status:** Proposed | Accepted | Superseded by NNNN
- **Date:** YYYY-MM-DD

## Context

The design problem and constraints.

## Decision

The chosen design and why.

## Alternatives Considered

Other options and why they were not chosen.
```

## Recorded DDRs

- `0001` — Read-only dashboard layout and data-loading state pattern
- `0002` — Model connection/load outcome as a discriminated-union IPC result
- `0003` — Snapshot persistence model, money representation, and capture policy
- `0004` — Flex import persistence: real-valued multi-currency amounts, append-only, two-tier de-dupe
- `0005` — M3 analytics read model, base-currency conversion, and per-period aggregation
- `0006` — App shell: client-side tab navigation for analytics views
- `0007` — Portfolio display currency: live IBKR FX source + selector/display conventions
- `0008` — Performance view: daily portfolio-value reconstruction from Flex MTM history
- `0009` — Sector classification: gateway-sourced, locally cached; allocation as donut charts
- `0010` — Upcoming dividends from Flex open accruals; net-of-withholding chart reading
- `0011` — Custom frameless window shell with in-app title bar and IPC-driven controls
- `0012` — In-place confirmation pattern for destructive actions (ConfirmAction)
- `0013` — Performance view: cumulative TWR curve, chart tabs, and shared interactive hover
- `0014` — Allocation view: geographic exposure as an equirectangular bubble map *(superseded by 0019 on the basemap; data semantics still apply)*
- `0015` — Allocation view: cash as an asset class, derived from the NAV residual
- `0016` — Dividend rows: shares held reconstructed from trades; per-share divided out of the amount
- `0017` — Analytics tables: time-range filter anchored to the data, composed with the type filter
- `0018` — One shared content measure for the shell; charts sized by aspect ratio
- `0019` — Allocation map: Mapbox basemap with an SVG bubble overlay (supersedes `0014`) *(superseded by 0020 on the overlay; basemap decisions still apply)*
- `0020` — Allocation map: one canvas circle per holding, with a hover popup (supersedes `0019`) *(superseded by 0030 on the unit, spread, canvas layer and per-holding granularity; approximation and a11y stance still apply)*
- `0021` — Allocation map: the gain/loss colour scale (red↔gray↔blue, fixed ±25%; extends `0020`) *(superseded by `0045`; its `--pos`/`--neg` rule still applies)*
- `0022` — Bounded gateway requests; a stalled gateway as its own `not_responding` state (extends `0002`)
- `0023` — Classification refresh: resumable (persist before reporting) and progress-reporting (extends `0009`)
- `0024` — Gateway reads coalesced in the repository behind a short freshness window; FX fetched concurrently (extends `0022`)
- `0025` — Single-instance lock: the second launch quits before touching the database and focuses the first (extends `0011`)
- `0026` — The Flex store describes itself: stored statements + coverage span, read on launch (extends `0004`)
- `0027` — Analytics tabs stay mounted; a re-read becomes explicit and non-destructive (extends `0006`)
- `0028` — Window size/position/maximized remembered in `app_meta`; reachability judged on the title bar (extends `0011`)
- `0029` — Tab shell: the complete ARIA tabs pattern, roving tabindex, automatic activation (extends `0006`)
- `0030` — Allocation map: two donuts per country (country weight + sectors); per-holding marks retired; palette blue reserved from sectors (supersedes `0020`)
- `0031` — Spacing, radius, type and focus scales on `:root`; one focus ring as a zero-specificity base rule (extends `0018`)
- `0032` — One `Button` primitive: seven role-named variants, four sizes, `className` for placement only (extends `0031`)
- `0033` — One `Card` primitive: `default`/`nested` surfaces, `--surface-pad-*` sizes, sentence-case titles, `CardContent` as the body scope (extends `0032`)
- `0034` — One `StatTile`: no surface of its own (the card supplies it), `tone` as the only axis, neutral as the absence of a tone (extends `0033`)
- `0035` — One `Field` owning an explicit `htmlFor` and a generated id; one `.control` box for `Select`/`DateInput`, `kind` as the only axis (extends `0034`)
- `0036` — One `ToggleGroup`: `mode` as the only axis (single-select box vs multi-select pill), `aria-pressed` buttons replacing three tablists that never implemented the pattern (extends `0035`)
- `0037` — One `Badge`: `variant` as boundary and ink, `size` as inline chip vs standalone label; never a pill, because `0036` spent that corner on "pressable" (extends `0036`)
- `0038` — One `StatePanel`: `variant` as the state (loading/empty/notice/error), `surface` as "does it bring a card"; element and ARIA role derived, only `error` paints (extends `0037`)
- `0039` — One `DataTable`: `surface`/`height` on the container (retiring the `.card-content` override), sorting opt-in per column, missing values last in both directions (extends `0038`)
- `0040` — Allocation breakdown: table and donut link on hover; emphasis keyed on slice identity, muting rather than recolouring (extends `0039`)
- `0041` — Map popup return tint: banked into the edges as a gradient whose band is the content padding, so colour never passes behind text (extends `0030`, `0021`)
- `0042` — Token adoption held by a ratchet: `BASELINE` may only shrink and a dead entry fails; exemptions enumerated by hand, because two mechanical rules leaked. Amended by #152, which emptied the baseline and exempted three sub-4px hairlines (extends `0031`)
- `0043` — One `AnalyticsShell` for the four analytics views: a render prop over the report, `subject` as the axis, branch mapping and state wording in a pure module (extends `0038`, `0027`)
- `0044` — Two durations, two easings; reduced motion honoured by redefining the tokens to `0ms` rather than listing what moves, so a raw duration is the only way out (extends `0031`)
- `0045` — Allocation map: one view, coloured by sector; the gain/loss mode and the `--diverge-*` scale withdrawn, the popup's return tint kept (supersedes `0021`, whose `--pos`/`--neg` rule still governs)
- `0046` — Contrast: the loss tone splits (`--neg` for fills, `--neg-text` for text) and `--accent-strong` fills the primary button, so neither validated palette moves; guarded by a Node test that measures hover states too (extends `0021`, `0034`, `0032`)
- `0047` — The Allocation map is a labelled `group`, not an `img`: it must contain Mapbox's attribution links, so the graphics are made inert instead (canvas and markers `tabindex="-1"`, `keyboard: false`); guarded by a comment-stripped text scan (extends `0030`, `0046`)
- `0048` — Tab icons are a second channel beside the label (never its name), one shared `Glyph` frame, sized `1em` from the type scale, drawn in `currentColor`; guarded by a comment-stripped text scan (extends `0029`, `0031`)
- `0049` — Performance daily-return bars: chain-linked from the TWR curve (never differenced from value), thinned rather than aggregated because aggregation destroys the volatility the chart exists to show; own component sharing `columnDomain` and the 4.5:1 ratio (extends `0013`, `0018`)
- `0050` — Daily NAV comes from `EquitySummaryInBase` (already exported, never parsed), so the value curve is the broker's series rather than `0008`'s reconstruction, which stays as the fallback; composition has negative bands below the baseline and an `other` residual that is surfaced, not redistributed — its 100%-stacked axis is superseded by `0052`, the rest stands (supersedes `0008`; extends `0005`, `0015`, `0018`)
- `0051` — Performance: the four charts in a 2×2 grid replacing the switcher; the shared plot re-cut to 500×180 (≈2.78:1) so a half-column keeps its axis labels legible, collapsing to one column at 1200px; geometry shared in `lib/chartGeometry` and checked by a walk over the layout (extends `0018`, `0049`, `0050`) Amended by #190, which found the y-axis gutter had never fitted the label it was sized for — 64 units buys eight characters and every value chart labels its axis with ten, so the `€` was clipped off every tick; now 80, derived from the measured advance and a stated character budget
- `0052` — Composition stacks cumulatively in base currency (top edge = NAV), bands softened by `fill-opacity` on `.stack-band` alone rather than retuning the palette; its legend moves into the card header so all four grid cards are exactly one header + one bare `<svg>` tall; daily-return bars scrubbed by band with an outlined active bar; the two chart source notes deleted (supersedes the 100%-stacked half of `0050`; extends `0051`, `0049`, `0030`)
- `0053` — Inter and JetBrains Mono bundled as repo assets (no font host, no CSP change, no dependency); a figure role that is **one rule** carrying family + `tabular-nums` + `-0.02em` together, its membership taken from the selectors that already declared tabular digits, minus four that render prose (extends `0031`, `0042`, `0018`)
- `0054` — Palette re-keyed to navy/indigo by measurement, not paste: four of the proposal's eleven values failed a guard and were re-derived (`--muted` 3.89:1, `--accent` 4.14:1 as text, and one rose that is right for neither role); the fill/text loss split survives and *inverts*; the eight categorical hues are re-validated and deliberately unchanged, while the three warm achromatic tokens go cool; five rules that hard-coded a colour now mix it from a token (extends `0046`, `0030`, `0031`)
- `0055` — The tablist rotates into a vertical 220px sidebar and stays a tablist: `aria-orientation="vertical"` and Up/Down, Left/Right deliberately inert, views still mounted rather than conditionally rendered; the active row's 2px underline becomes a 3px leading-edge bar plus a weight step, the hover rule is scoped `:not(.app-tab-active)` because `:hover` out-specifies it, and `lib/contrast.ts` learns to measure a `color-mix()` tint. The Performance grid's breakpoint moves 1200px → 1420px to keep meaning the same content column, which costs DDR-0051's "a fresh install opens on the grid" (extends `0029`, `0027`, `0048`, `0051`, `0054`)
- `0056` — The sidebar's context rail: the gateway badge is **derived** from the Portfolio view's own `portfolio:getOverview` result rather than polled (no new channel, no timer against the gateway), and a live reading ages out at five minutes — `Live · 14:32` becomes `Last seen 14:32` — mirroring the repository's session TTL; five outcomes get five wordings against three tones, so no pair is separated by colour alone, and the dot's fill/text split is declared once as two local properties. The display currency moves to the footer and becomes the *app's* selection: never disabled, sequenced instead, and it stops resetting to EUR on every visit. The prototype's account number is deliberately not shown — the overview carries none, and a new figure opens its own story (extends `0055`, `0022`, `0024`, `0027`, `0046`)
- `0057` — Sidebar collapse: one `app-collapsed` flag on the shell rather than a `collapsed` prop anywhere, so the tabs pattern is the same code in both states and selecting a view *cannot* reopen the column; the title bar keeps spanning the window above the sidebar, because with no OS frame it is the only grab handle and `0028` judges reachability on it; collapsed labels are **clipped, not removed**, so a rail row is still named by its own text and `title` stays an addition; the status dot grows a shape channel (filled/hollow/haloed) where its wording is clipped; state persisted as one `app_meta` value beside the window's own, and the grid's two breakpoints derived from one 1200px content column — which gives the 1280px default window its 2×2 grid back (extends `0055`, `0056`, `0011`, `0028`, `0029`, `0044`, `0051`) *(superseded by `0068` on the toggle's placement; everything else still applies)*
- `0058` — One page header for all five views, owned by `AnalyticsShell` (which takes the `<main>` with it) rather than by `App.tsx`'s `AnalyticsPage` — so the title renders in every branch by construction, error and needs-import included; `RefreshBar` dissolves into the header's status row, Portfolio gains a reading time it never had, the status row is absent (not empty) where there is nothing to refresh, and the prototype's repeated app-name eyebrow is dropped because the title bar and the brand tile already carry it (extends `0043`, `0027`, `0038`, `0056`)
- `0059` — The card's ruled header strip: one rule over two selectors (a `CardHeader` and a bare `CardTitle` read identically), bled to the card's edges by negating a `--card-pad` each size restates, with `.card-header:last-child` giving it all back; `CardContent` untouched, so no `StatePanel` moves. The table's column head drops to 11px with the tracking that makes 11px uppercase survive, and every table gains a CSS row lift on `--duration-fast` — the linked row's stronger lift re-scoped to out-weigh it, since the two are the same row whenever the pointer is what linked it. The proposal's `20px 24px` padding and its `--surface-2` are both declined (extends `0033`, `0039`, `0040`, `0044`, `0054`)
- `0060` — The KPI tile's figure goes to the redesign's 26px and the row's column minimum moves with it — one decision in two declarations, with the arithmetic (0.6em digits × a twelve-character figure + two card paddings) asserted rather than commented, and 14.5rem the middle of a 0.4rem window bounded below by the card overrunning and above by Trades' four tiles leaving one row at 1280px; it costs a column at the minimum window, which `auto-fit` is for. `.stat-label` adopts `0059`'s 11px micro-label rather than the proposal's second tracking, so the app has one; the tone axis, the absent surface and the figure role are all untouched (extends `0034`, `0031`, `0059`, `0053`)
- `0061` — The chart language: the area wash becomes a `<linearGradient>` whose stops stay CSS (the `fill` is per-element, because a gradient is referenced by id and two curves share a page), and `LineChart` gains a zero line only where zero is a level the series *crosses* rather than the floor it sits on. Three private `HoverReadout`s become one `ChartTooltip` drawn **inside the `viewBox`** — which is what makes "it cannot cover the chart next door" a construction rather than a tuned offset — pinned to the top of the plot in all three, with a muted sans label beside a right-aligned figure. Bars, `.stack-band`, the donut's native tooltip and the map are all deliberately untouched (extends `0018`, `0051`, `0052`, `0053`, `0044`)
- `0062` — The Portfolio view's rail: the imported store renders *beside* the live states rather than inside the `ok` branch, so a disconnected gateway can't hide the import path; `FlexImport` becomes a hook plus three fragments with the `dataVersion` bump in the hook, where splitting the panel across three grid cells cannot strand it; `SnapshotHistory` survives the prototype's omission and pairs with Stored statements as the app's two local stores; and one `lib/weightBars` scale — the set's maximum with a 25% floor under it — replaces the prototype's two disagreeing hard-coded divisors, drawing the rail's `meter` and the table's `aria-hidden` micro-bar from the same number (extends `0027`, `0012`, `0046`, `0057`)
- `0063` — Allocation's breakdown becomes a *pair*: the donut's column fixed at 280px (a third structural width token) with the table taking every pixel the window gains, `align-items: start`, and a stacking breakpoint derived from what the table needs (948px, asserted rather than commented); the donut's legend returns **wrapping**, carrying a muted name and its share while value stays in the table and the slice's `<title>` — the `showLegend` prop and the vertical legend it guarded were both dead. The breakdown moves above the map, because the map is the tallest card and the only approximate one. The basemap goes `dark-v11`, the decision `0054` deferred here, and it is **two** changes: the weight donut's track was `var(--card)` at 42%, which is a grey ring only when a white map is behind it, so it moves to `--muted` — asserted together, since either alone is a regression that looks deliberate (supersedes `0019`'s style value, not its reasoning; extends `0030`, `0040`, `0054`)
- `0064` — The prototype's tinted type chips ship as two `Badge` variants that are still a boundary and an ink, not a fill: the tone mixed halfway into `--border`, `--pos` and `--neg-text` as the two inks, and no weight step because a type label cannot stop naming its type. The variant union becomes a **superset** of `STAT_TONES`, so `toneOf()` names a variant directly — and the polarity comes from the row's **signed amount**, never from a list of Flex type strings that is right today and silent when a fourth type arrives. The badge is `sm` (a `md` box grows every row) with the inline gap taken off by a doubled placement class, because the two forms are indistinguishable to CSS: an inline chip follows a *text node*, and `:first-child` counts elements. The income key moves into the card header and `ColumnChart` loses its `<figcaption>` (extends `0037`, `0046`, `0034`, `0052`, `0059`)
- `0065` — Trade side becomes a badge with **no tone at all**: the gain/loss pair is declined because those two hues already mean gain and loss in the same row's last cell, and no substitute hue is invented either — `--accent` means news (`0037`) and the `--series-*` slots are a dimension's categorical set (`0030`), so the badge earns its place on **shape**, a run of boxes where a bare word left the column ragged. No new variant, no new contrast pairing, and no branch on the side string. `.flex-import-dim` moves to `.data-table .data-table-dim` rather than gaining a second copy under a second view's prefix, and the `realizedNative !== 0` predicate written out three times becomes one, so the column cannot tone a row it renders as a dash. The `Best`/`Worst` label is toned by **its own item's figure**, not by which card it labels — composed from `.stat-label` plus the tone rules rather than declared as a third — and the nested card's separation is re-measured and left alone: it was always the border (1.228 → 1.252:1), never the fill (extends `0037`, `0064`, `0034`, `0033`, `0039`, `0021`)
- `0066` — One instrument name across Dividends, Trades and Allocation, decided by whether the description **says anything the symbol does not** rather than by its shape or its asset category: the story proposed a `XXX.YYY` regex for currency pairs, and querying the owner's real statements found a *second* non-company shape — bare codes (`CAD`, `USD`) in `flex_fifo_summaries` that carry realized P&L and so already sit in the realized-gains table — which that regex would have rendered as "Cad". `assetCategory` is declined for `0064`'s reason and because `RealizedBySymbol` does not carry one. `instrumentName` returns **`null`**, not the raw string, so a line repeating the ticker above it is not drawn at all; one four-line `InstrumentName` holds the app's only remaining `.flex-import-file` inside `components/analytics/`, asserted against the **directory** so a fourth view cannot reintroduce the split. The suffix list's `SPA` and `GROUP` judgements are now visible on every instrument and deliberately unchanged (extends `0064`, `0017`, `0029`)
- `0067` — The suffix list was one list doing two jobs, so it becomes two: a **legal form** is never part of a name and goes wherever it trails, repeatedly; a **common noun** (`GROUP`, `HOLDINGS`, `COMPANY`, `CO`) often *is* the name and goes **only where it is the last word** — the owner's call, and the only rule available, since `GOLDMAN SACHS GROUP INC` and `JUROKU FINANCIAL GROUP INC` reach opposite verdicts and a name with a word too many is still that company while a name with a word too few is a different one. Order is load-bearing: the noun is stripped **once, before** any legal form, or removing `INC` promotes `GROUP` into last place and takes it anyway. A stranded leading `THE` goes with the noun it paired with (and only where something *was* stripped, so `THE TORONTO-DOMINION BANK` keeps it); an acronym is a token with **no vowel**, because the case that would tell `LVMH` from `GOLD` is the case being replaced — misses (`AB`, `SAP`) asserted rather than hidden. `SPA` stays a legal form on evidence: two real Italian holdings against a hypothetical spa. Two of seventeen real instrument names change, both `GROUP` retentions, checked by running both versions over the real database (extends `0066`, `0064`)
- `0068` — The collapse toggle moves out of the footer into the sidebar's **head row**, beside the app's name: it is the one control that changes the navigation's width and it sat at the opposite end of the column from it. `0057` measured that placement rather than preferring it — 118px beside a 32px tile and a 30px control, where "Stock Portfolio Viewer" wants 154 — so the measurement is answered rather than ignored: the name **wraps to two lines** on `--leading-tight` and the head grows ~9px, because an abbreviated product name is a worse defect than a taller head row. Widening the column is refused, since `--sidebar-width` is an input to *both* Performance breakpoints and a wider one costs the 1280px window the 2×2 grid collapsing exists to give it back; on the rail the row turns the corner and stacks the tile above the control. The Tab order becomes toggle → tabs → currency → panel — one stop *fewer* between a tab and its panel, not one more (supersedes `0057`'s placement only; extends `0056`, `0055`, `0029`, `0051`)
